version: '3'
services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
      - "28017:28017"
  reactjs:
    image: banjocat/outage_reactjs
    build: ./reactjs/.
    volumes:
      - './reactjs/nginx.conf:/etc/nginx/conf.d/default.conf'
    environment:
      EXPRESS_HOST: "express:3000"
    ports:
      - "8001:80"
    depends_on:
      - express
  express:
    image: banjocat/outage_express
    build: ./express/.
    volumes:
      - "./express/index.js:/app/index.js"
    environment:
      mongo_url: mongodb
      express_port: 3000
    ports:
      - "3000:3000"
    depends_on:
      - mongodb


  

